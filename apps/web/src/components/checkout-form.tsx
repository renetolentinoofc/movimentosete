"use client";
import { useRef,useState } from "react";
import { useRouter } from "next/navigation";
import type { Envelope } from "@/lib/api";
type Order={order_code:string;access_token:string;status:string;payment_status:string;message:string};
export function CheckoutForm(){const router=useRouter();const key=useRef(crypto.randomUUID());const [status,setStatus]=useState<"idle"|"submitting"|"error">("idle");const [message,setMessage]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setStatus("submitting");const form=new FormData(event.currentTarget);const v=Object.fromEntries(form);const body={customer:{name:v.name,email:v.email,phone:v.phone},address:{recipient_name:v.recipient_name,postal_code:v.postal_code,street:v.street,number:v.number,complement:v.complement,neighborhood:v.neighborhood,city:v.city,state:v.state},terms_accepted:v.terms_accepted==="on",terms_version:"2026-08-draft"};const response=await fetch("/api/checkout",{method:"POST",headers:{"Content-Type":"application/json","Idempotency-Key":key.current},body:JSON.stringify(body)});const payload=await response.json() as Envelope<Order>;if(!response.ok||!payload.data){setMessage(payload.error?.message??"Não foi possível criar o pedido.");setStatus("error");return;}router.push(`/pedido/${payload.data.order_code}?token=${encodeURIComponent(payload.data.access_token)}`)}return <form className="form" onSubmit={submit}>{status==="error"&&<div className="error-summary" role="alert">{message}</div>}<fieldset className="card"><legend className="legend">Contato</legend><div className="field"><label htmlFor="checkout-name">Nome</label><input id="checkout-name" name="name" required autoComplete="name"/></div><div className="field"><label htmlFor="checkout-email">E-mail</label><input id="checkout-email" name="email" type="email" required autoComplete="email"/></div><div className="field"><label htmlFor="checkout-phone">WhatsApp</label><input id="checkout-phone" name="phone" required autoComplete="tel"/></div></fieldset><fieldset className="card"><legend className="legend">Endereço</legend>{[["recipient_name","Destinatário"],["postal_code","CEP"],["street","Rua"],["number","Número"],["complement","Complemento"],["neighborhood","Bairro"],["city","Cidade"],["state","UF"]].map(([name,label])=><div className="field" key={name}><label htmlFor={`checkout-${name}`}>{label}</label><input id={`checkout-${name}`} name={name} required={name!=="complement"} maxLength={name==="state"?2:180}/></div>)}</fieldset><label><input type="checkbox" name="terms_accepted" required/> Li os termos, política de privacidade e informações de entrega. Entendo que criar o pedido não aprova pagamento.</label><button className="button" disabled={status==="submitting"}>{status==="submitting"?"CRIANDO PEDIDO…":"CRIAR PEDIDO"}</button></form>}
