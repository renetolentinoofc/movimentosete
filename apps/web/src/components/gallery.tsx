"use client";
import { ChevronLeft,ChevronRight,X } from "lucide-react";
import { useEffect,useRef,useState } from "react";
type Media={id:string;album:string;edition:string;category:string;type:string;url:string;title:string;caption?:string;alt:string;credit?:string;width?:number;height?:number};
const filters=["Todos","Eventos","Barbeiros","Artistas","Skate","Grafite","MCs","Moda","Bastidores"];
export function Gallery({items}:{items:Media[]}){const [filter,setFilter]=useState("Todos");const [active,setActive]=useState<number|null>(null);const closeRef=useRef<HTMLButtonElement>(null);const visible=filter==="Todos"?items:items.filter(item=>item.category.toLowerCase()===filter.toLowerCase());useEffect(()=>{if(active!==null){closeRef.current?.focus();history.replaceState(null,"",`?midia=${visible[active]?.id}`)}else history.replaceState(null,"",location.pathname);},[active,visible]);useEffect(()=>{const key=(event:KeyboardEvent)=>{if(active===null)return;if(event.key==="Escape")setActive(null);if(event.key==="ArrowRight")setActive((active+1)%visible.length);if(event.key==="ArrowLeft")setActive((active-1+visible.length)%visible.length)};addEventListener("keydown",key);return()=>removeEventListener("keydown",key)},[active,visible.length]);return <><div role="group" aria-label="Filtrar galeria" className="grid cards">{filters.map(name=><button className={filter===name?"button":"button secondary"} key={name} type="button" aria-pressed={filter===name} onClick={()=>{setFilter(name);setActive(null)}}>{name}</button>)}</div><div className="grid cards" style={{marginTop:"2rem"}}>{visible.map((item,index)=><button className="card" style={{textAlign:"left",color:"inherit"}} key={item.id} type="button" onClick={()=>setActive(index)}><img src={item.url} alt={item.alt} width={item.width??800} height={item.height??600} loading="lazy"/><h2 style={{fontSize:"1.6rem"}}>{item.title}</h2><p>{item.album}</p></button>)}</div>{!visible.length&&<div className="empty">Nenhuma mídia publicada neste filtro.</div>}{active!==null&&visible[active]&&<div role="dialog" aria-modal="true" aria-label={visible[active].title} style={{position:"fixed",inset:0,zIndex:100,background:"rgb(0 0 0 / 92%)",padding:"1rem",display:"grid",placeItems:"center"}}><div className="card" style={{maxWidth:"70rem",maxHeight:"95vh",overflow:"auto"}}><button ref={closeRef} className="button secondary" onClick={()=>setActive(null)} aria-label="Fechar visualização"><X aria-hidden/></button>{visible[active].type==="video"?<video controls preload="metadata" src={visible[active].url}/>:<img src={visible[active].url} alt={visible[active].alt}/>}<h2 style={{fontSize:"2rem"}}>{visible[active].title}</h2>{visible[active].caption&&<p>{visible[active].caption}</p>}{visible[active].credit&&<p className="muted">Crédito: {visible[active].credit}</p>}<div><button className="button secondary" onClick={()=>setActive((active-1+visible.length)%visible.length)} aria-label="Mídia anterior"><ChevronLeft/></button> <button className="button secondary" onClick={()=>setActive((active+1)%visible.length)} aria-label="Próxima mídia"><ChevronRight/></button></div></div></div>}</>}
