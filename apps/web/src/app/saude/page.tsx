export const dynamic = "force-dynamic";
export default async function HealthPage() { let online=false; try { const response=await fetch(`${process.env.INTERNAL_API_URL ?? "http://127.0.0.1:5000"}/api/v1/health/live`, {cache:"no-store"}); online=response.ok; } catch {} return <section className="section"><div className="container"><p className="eyebrow">Status</p><h1>Saúde do serviço</h1><p className="lead">Site: disponível</p><p>API: <span className="status">{online ? "disponível" : "temporariamente indisponível"}</span></p><p className="muted">Nenhum segredo ou detalhe de infraestrutura é exibido nesta página.</p></div></section>; }
