import { CheckoutForm } from "@/components/checkout-form";
export default function CheckoutPage(){return <section className="section"><div className="container"><p className="eyebrow">Loja</p><h1>Checkout</h1><p className="lead">O servidor recalcula preço e estoque. No modo manual, nenhum pagamento é marcado como aprovado.</p><CheckoutForm/></div></section>}
